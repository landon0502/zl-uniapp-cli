/**
 * interface.uts
 * uts插件接口定义文件，按规范定义接口文件可以在HBuilderX中更好的做到语法提示
 */

/**
 * 中文转拼音函数
 * 将中文转换为拼音字符串
 * @param text 要转换的中文字符串
 * @returns 转换后的拼音字符串
 */
export type ChineseToPinyin = (text: string) => string