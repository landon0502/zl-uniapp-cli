{
  "name": "zl-uniapp-cli",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @zlskuniapp/zlapp-cli dev",
    "test": "pnpm -r test",
    "release": "pnpm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.3"
  }
}
